const validation = require("./validation");
const controllerWrapper = require("./contarollerWrapper");
const auth = require("./auth");
const upload = require("./upload");

module.exports = {
  validation,
  controllerWrapper,
  auth,
  upload,
};
